이진트리: 모든 노드의 차수 개수 <= 2 == 모든 노드가 2개의 서브트리를 가짐
       * 왼쪽 자식, 오른쪽 자식
       # 정 이진 트리
       # 포화 이진 트리
       # 완전 이진 트리
       # 균형 이진 트리
       # 편향 트리

       -> 배열 구현: 완전 이진 트리
       -> 연결 리스트 구현

       struct Node {
         char data;
         Node* left;
         Node* right;
         Node(char d) : data, left(nullptr), right(nullptr)
       };
